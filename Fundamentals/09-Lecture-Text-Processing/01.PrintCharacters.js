function printChar(input) {
    for (let char of input) {
        console.log(char);
    }
}

printChar('AWord');

function printCharTwo(input) {
    for (let i = 0; i < input.length; i++) {
        console.log(input[i]);
    }
}

printCharTwo('AWord');